using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaybackInterseccion : MonoBehaviour
{
    /*
    public int id;

    public void InicializarInterseccion(EstadoInicialInterseccion estadoInicialInterseccion)
    {
        var interseccion = GetComponent<Interseccion>();
        float delay = estadoInicialInterseccion.segundosActivada + 2; // +2 porque, como la corrutina que pone
        //el semaforo en verde espera 1 segundo hasta desactivar el collider hay que considerar que tarda 1 segundo mas,
        //ademas al inicializar la simulacion se ponen todos los semaforos en rojo lo cual tambien tarda un segundo.

        interseccion.StartCoroutine(interseccion.InicInterseccionDelayed(delay));
    }
    */
}
